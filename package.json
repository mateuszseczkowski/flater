{
  "name": "flater",
  "version": "1.0.5",
  "description": "Gets data from pages with apartments and saves the data into the csv file",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "flat",
    "apartment"
  ],
  "author": "MS",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "puppeteer": "^1.13.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.4"
  }
}
